const userRoute = require('./user.route');
const musicianRoute = require('./musician.route');
const eventRoute = require('./event.router');

module.exports = {
  userRoute,
  musicianRoute,
  eventRoute,
};
